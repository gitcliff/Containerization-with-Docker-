# 📦 SUMMARY  
# This step accesses the Mongo Express web UI running in a Docker container.  
# Language/Platform: Browser & Web UI (Mongo Express)  
# What you'll learn: How to connect to a containerized service from your host machine  
# Outcome: You log into Mongo Express and manage your MongoDB instance visually  
# Why this matters: Simplifies database inspection and testing without needing CLI commands  

# ------------------------------  
# 🧩 SECTION TITLE: Access Mongo Express Web UI  
# ------------------------------  

# ✅ What it does: Opens the Mongo Express admin interface in your web browser  
# 🧠 Why it matters: Provides a GUI to interact with MongoDB (view databases, collections, documents, etc.)  

# 📂 URL to visit in browser:  
http://localhost:8081  
# ✅ What it does: Accesses Mongo Express running in the container on host port 8081  
# 🧠 Why it matters: Docker port mapping exposes Mongo Express from the container to your local system  

# 📝 Login credentials for Mongo Express:  
Username: admin  
# ✅ What it does: Root username configured in docker-compose.yaml (ME_CONFIG_MONGODB_ADMINUSERNAME)  

Password: pass  
# ✅ What it does: Root password configured in docker-compose.yaml (ME_CONFIG_MONGODB_ADMINPASSWORD)  

# ------------------------------  
# ✅ NOTES / GOTCHAS / TIPS  
# ------------------------------  
# - If http://localhost:8081 doesn’t load:  
#   - Check container status: `docker ps`  
#   - Verify port mapping (8081:8081) in docker-compose.yaml  
#   - Ensure no firewall blocks port 8081  
# - Mongo Express UI lets you:  
#   - Browse DBs  
#   - Add/update/delete documents  
#   - Run queries visually  
